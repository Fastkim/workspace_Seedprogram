// component ==> ui piece
// component를 js 객체로 표현 ==> frame
// presentation logic + appearance
export default function Button() { //export default: 모듈을 내보내기중에, 해당모듈엔 개체가 하나만 있다라는 사실을 의미함
    return (
        <button>I'm a button.</button>
    )
}
// HTML 코드가 아니다.