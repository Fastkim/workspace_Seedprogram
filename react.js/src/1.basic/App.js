import State from './/6.State'

export default function App() {
    return (
        <State/> // react component button
    )
}

// RESTful app ==> representational state(data) transfer
// representational state를 json으로 표현 ==> transfer
// 각각의 자원을 URI로 구분한다.
// CRUD ==> POST/GET/PUT or PATCH/DELETE